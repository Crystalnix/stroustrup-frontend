import { StyleSheet } from 'aphrodite'

const styles = {
  container: {
    marginTop: '20px',
  },
  text: {
    marginTop: '10px',
  },
}

const styleSheet = StyleSheet.create(styles)

export default styleSheet
